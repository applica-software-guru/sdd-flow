# Image publication/deploy template
REGISTRY=ghcr.io
IMAGE_NAMESPACE=your-org
IMAGE_NAME_BACKEND=sdd-flow-backend
IMAGE_NAME_FRONTEND=sdd-flow-frontend
IMAGE_TAG=latest
BACKEND_IMAGE=${REGISTRY}/${IMAGE_NAMESPACE}/${IMAGE_NAME_BACKEND}:${IMAGE_TAG}
FRONTEND_IMAGE=${REGISTRY}/${IMAGE_NAMESPACE}/${IMAGE_NAME_FRONTEND}:${IMAGE_TAG}

# Backend runtime
DATABASE_URL=
JWT_SECRET=
APP_DOMAIN=
FRONTEND_URL=
ENABLE_GOOGLE_OAUTH=false
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAMESITE=lax

# Frontend runtime
NGINX_SERVER_NAME=_
BACKEND_UPSTREAM=backend:8000
PUBLIC_APP_DOMAIN=
